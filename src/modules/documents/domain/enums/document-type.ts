export enum DocumentType {
  Announcement = 'ANNOUNCEMENT',
  AssemblyMinutes = 'ASSEMBLY_MINUTES',
  AssemblyNotice = 'ASSEMBLY_NOTICE',
  Other = 'OTHER',
  /** Inventário LGPD interno; gerado/atualizado só via sync. */
  DataInventory = 'DATA_INVENTORY',
}
